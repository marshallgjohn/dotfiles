# Oh My Zsh is installed by install.sh to ~/.oh-my-zsh — do not vendor it
.oh-my-zsh/
# zsh completion caches
.zcompdump*
*.zwc
# editor / OS noise
.DS_Store
*~
# LazyVim local state (version stamps, news read-marks)
.config/nvim/lazyvim.json
